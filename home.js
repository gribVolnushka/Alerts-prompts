alert('Spring in ___  is great to spend time in ___ ');

const firstWorld = prompt('Enter the first world');
const secWorld = prompt('Enter the second world');

if (firstWorld && secWorld) {
    alert('Spring in ' + firstWorld + ' is great to spend time in ' + secWorld + ' .');
} else  {
    alert('ERROR');
}

